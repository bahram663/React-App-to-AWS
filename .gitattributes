# Normalise to LF in the repository. The workflow `run:` blocks execute in bash
# on ubuntu-latest, where a stray CR breaks heredocs and line continuations.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
